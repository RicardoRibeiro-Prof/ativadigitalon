const toggle=document.getElementById('searchToggle');
const panel=document.getElementById('searchPanel');
const input=document.getElementById('newsSearch');
const FALLBACK_IMAGE='data:image/svg+xml;charset=UTF-8,'+encodeURIComponent(`<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 450"><rect width="800" height="450" fill="#ececea"/><text x="400" y="225" text-anchor="middle" font-family="Arial" font-size="32" font-weight="700" fill="#777">PORTAL SERRA ATUAL</text><text x="400" y="265" text-anchor="middle" font-family="Arial" font-size="19" fill="#888">Imagem indisponível</text></svg>`);
function applyImageFallbacks(root=document){root.querySelectorAll('img').forEach(img=>{if(img.dataset.fallbackReady)return;img.dataset.fallbackReady='1';img.addEventListener('error',()=>{if(img.src===FALLBACK_IMAGE)return;img.src=FALLBACK_IMAGE})})}
toggle?.addEventListener('click',()=>{panel.classList.toggle('open');if(panel.classList.contains('open'))input.focus()});
input?.addEventListener('input',e=>{const q=e.target.value.toLowerCase().trim();document.querySelectorAll('.news-card').forEach(card=>{const t=((card.dataset.search||'')+' '+card.innerText).toLowerCase();card.style.display=q&&!t.includes(q)?'none':''})});
document.querySelectorAll('[data-category]').forEach(link=>{link.href='categoria.html?slug='+encodeURIComponent(link.dataset.category)});
function renderTopCampaign(){const ads=JSON.parse(localStorage.getItem('serra_ads')||'[]');const ad=ads.find(x=>x.status==='Ativa'&&x.position==='Banner superior'&&(x.desktopImage||x.image));const slot=[...document.querySelectorAll('.ad.wrap')].find(el=>el.textContent.includes('Banner superior'));if(!slot)return;if(!ad){slot.style.display='none';return}const desktop=ad.desktopImage||ad.image;const mobile=ad.mobileImage||desktop;slot.classList.add('real-ad');slot.style.display='block';slot.innerHTML=`<a href="${ad.link||'#'}" ${ad.link?'target="_blank" rel="noopener"':''}><picture><source media="(max-width:620px)" srcset="${mobile}"><img src="${desktop}" alt="${ad.name||'Publicidade'}"></picture></a>`;applyImageFallbacks(slot)}
renderTopCampaign();applyImageFallbacks();
const marquee=document.querySelector('.logo-track');if(marquee){marquee.style.animationPlayState='running';['mouseenter','mouseleave','touchstart','touchend','pointerenter','pointerleave'].forEach(name=>marquee.addEventListener(name,()=>marquee.style.animationPlayState='running',{passive:true}))}